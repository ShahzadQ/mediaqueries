import type { MediaTypeExpandedVariants } from '@/types';

export const _mediaTypes: MediaTypeExpandedVariants[] = [
  'screen',
  'print',
  'all',
  'only screen',
  'only print',
  'only all',
  'not all',
  'not print',
  'not screen'
];
